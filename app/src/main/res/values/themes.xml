<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Snapeat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Snapeat" parent="Base.Theme.Snapeat" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#45301f</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_snapeat</item>
        <item name="postSplashScreenTheme">@style/Theme.Snapeat</item>
    </style>
</resources>